{
	"name": "passwordgen",
	"version": "1.0.0",
	"main": "script.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "mkdir -p dist && npx tailwindcss -i ./src/style.css -o ./dist/style.css --minify",
		"dev": "tailwindcss -i ./src/style.css -o ./dist/style.css --watch",
		"clean-build": "rm -rf dist/ && npm run build"
	},
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"@tailwindcss/cli": "^4.0.17",
		"tailwindcss": "^4.0.17"
	}
}
